import random

# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")

# import random
# names_int = len(names)
# random_names = random.randint(0, names_int - 1) #-1 isliye because python starts from 0, but len starts from 1 because it is counting
# chosen_one = names[random_names]
# print(f"{chosen_one} has to pay the bill")

chosen_one = random.choice(names)
print(f"{chosen_one} has to pay the bill")
